#ifndef COMPILE_H
#define COMPILE_H

// enable logging (prints diagnostic information when running program)

const bool LOGGING = false;

// string printed to output before log message

const char LOGMSG[] = "LOG: ";

#endif
